# --- RENDER BACKEND CONFIG ---
PORT=5000
CORS_ORIGIN=https://your-vercel-domain.vercel.app
ERP_SCRAPER_MODE=live
# Path to persist the SQLite database (optional for Render Disk)
# DATABASE_PATH=/var/lib/data/attendance_sessions.sqlite

# --- SCRAPER OVERRIDES (Juno Defaults set in code) ---
# ERP_LOGIN_URL=https://erp.cmrit.ac.in/
# ERP_PUPPETEER_HEADLESS=true
